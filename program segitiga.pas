program segitiga;

// var dn,i:Integer;
var n,i,j:Integer;

begin
// Tugas 1
  read(dn);
  i:=1;
  write(dn, ' = ', i);
  repeat
    begin
      i:= i+1;
      write(' + ', i);
    end;
  until i >= dn;

  // n:=5;
  // for i:=1 to n do
  // begin
  //   for j:=1 to i do
  //     begin
  //       write('*');
  //     end;
  //   // write('*');
  //   writeln;
  // end;


end.